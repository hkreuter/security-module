{% include "email/html/header.html.twig" with {title: subject|default("OESM_PASSWORDCHANGE_EMAIL_SUBJECT"|translate)} %}

{# 'oesmpasswordchangeemail' mirrors PasswordChangeMailContent::IDENT; the notifier always passes contentIdent explicitly. #}
{# The content body is stored as plain text with newlines; nl2br turns them into <br> for the HTML mail. #}
{% apply nl2br %}
{%- include_content contentIdent|default('oesmpasswordchangeemail') with {changedAt: changedAt} -%}
{% endapply %}

{% include "email/html/footer.html.twig" %}
